#include"main.h"
/**
 *  _isdigit shows if a char is a digit
 *
 *  Description: 'the program's description'
 *  @c: char to check
 * Return: 1 isdigi 0 otherwise
 */
int _isdigit(char c)
{
if (c >= '0' && c <= '9')
return (1);
return (0);
}
