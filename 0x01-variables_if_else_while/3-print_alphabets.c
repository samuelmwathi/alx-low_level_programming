#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char b;
char a;
for (b = 'a'; b <= 'z'; b++)
putchar(b);
for (a = 'A'; b <= 'Z'; a++)
putchar(a);
putchar('\n');
return (0);
}
